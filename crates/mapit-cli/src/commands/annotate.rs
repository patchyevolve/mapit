use std::path::Path;
use anyhow::Result;
pub async fn run(_target: &Path, _all: bool, _force: bool) -> Result<()> {
    println!("mapit annotate — Phase 5 (AI provider layer not yet implemented)");
    Ok(())
}
