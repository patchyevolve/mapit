You are analyzing a codebase to identify feature groupings. Given the list of files and symbols below, group them into logical features/subsystems. Each item can belong to at most one group.

Project files:
{{file_list}}

Symbols:
{{symbol_list}}

Structural clustering hints:
{{clustering_hints}}

Respond with a JSON object:
{
  "features": [
    {
      "name": "feature_name",
      "confidence": 0.0-1.0,
      "member_symbol_ids": ["symbol_id", "..."],
      "member_file_ids": ["file_id", "..."]
    }
  ]
}
