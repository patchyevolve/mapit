You are analyzing source code for potential issues. Given the following function's code and structural context, identify any flaws.

Symbol name: {{name}}
File: {{file_path}}
Lines: {{start_line}}-{{end_line}}
Has incoming calls: {{has_incoming_calls}}
Is entry point candidate: {{is_entry_point_candidate}}

Signature:
{{signature}}

Source code:
```{{language}}
{{source_text}}
```

Callers:
{{callers}}

Callees:
{{callees}}

Only flag flaws you are confident about. The structural fact "has_incoming_calls = false" paired with "is_entry_point_candidate = false" is a genuine dead-code signal — assess whether the code looks like dead code or if there's a reason it might be reachable (e.g., registered as a callback, called via macro, public API).

Respond with a JSON object:
{
  "flaws": [
    {
      "kind": "dead_code" | "circular_dependency" | "structural_smell" | "suspected_bug" | "missing_error_handling" | "resource_leak_pattern",
      "severity": "info" | "warning" | "high",
      "description": "what the issue is",
      "confidence": 0.0-1.0,
      "basis": "structural+ai"
    }
  ]
}
